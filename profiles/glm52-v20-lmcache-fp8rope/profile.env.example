# Sanitized; replace local paths and the image reference privately. The live
# deployment uses a locally built image with the v20 source lineage recorded in
# manifest.json; do not publish private registry credentials or host paths.
IMAGE_DIGEST=REPLACE_WITH_V20_IMAGE_DIGEST
MODEL_DIR=/srv/models/GLM-5.2
CACHE_DIR=/srv/cache/glm52-v20-lmcache
L2_DIR=/srv/nvme/lmcache-l2
PORT=5810
TP=4
DCP=4
MTP=3
MAX_MODEL_LEN=400384
